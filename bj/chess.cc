#include <iostream>
using namespace std;

int main(){
    int a,b,c,d,e,f;
    cin >> a >> b >> c >> d >> e >> f;
    int a_diff = a-1;
    int b_diff = b-1;
    int c_diff = c-2;
    int d_diff = d-2;
    int e_diff = e-2;
    int f_diff = f-8;
    if(a_diff > 0){
        cout << -1*a_diff << " ";
    }
    else if(a_diff < 0){
        cout << -1*a_diff << " ";
    }
    else if(a_diff == 0){
        cout << "0 "; 
    }
    if(b_diff > 0){
        cout << -1*b_diff << " ";
    }
    else if(b_diff < 0){
        cout << -1*b_diff << " ";
    }
    else if(b_diff == 0){
        cout << "0 "; 
    }
    if(c_diff > 0){
        cout << -1*c_diff << " ";
    }
    else if(c_diff < 0){
        cout << -1*c_diff << " ";
    }
    else if(c_diff == 0){
        cout << "0 "; 
    }
    if(d_diff > 0){
        cout << -1*d_diff << " ";
    }
    else if(d_diff < 0){
        cout << -1*d_diff << " ";
    }
    else if(d_diff == 0){
        cout << "0 "; 
    }
    if(e_diff > 0){
        cout << -1*e_diff << " ";
    }
    else if(e_diff < 0){
        cout << -1*e_diff << " ";
    }
    else if(e_diff == 0){
        cout << "0 "; 
    }
    if(f_diff > 0){
        cout << -1*f_diff << " ";
    }
    else if(f_diff < 0){
        cout << -1*f_diff << " ";
    }
    else if(f_diff == 0){
        cout << "0 "; 
    }
    return 0;
}